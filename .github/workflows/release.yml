name: "release"
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checking out
        uses: actions/checkout@v3

      - id: releaser
        name: Configuration File Releaser
        uses: thiagodonferreira/configuration-file-releaser@main
        with:
          repository: thiagodonferreira/configuration-file-releaser
          loader: nodejs
          configurationFile: package.json

      - run: echo ${{steps.releaser.outputs.currentRelease}}

      - run: echo ${{steps.releaser.outputs.nextRelease}}

      - name: Create Release on Github
        uses: softprops/action-gh-release@v1
        if: ${{steps.releaser.outputs.currentRelease != steps.releaser.outputs.nextRelease}}
        with:
          name: ${{ steps.releaser.outputs.nextRelease }}
          tag_name: v${{steps.releaser.outputs.nextRelease}}
          # files: "*"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
